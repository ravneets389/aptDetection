# aptDetection
A web app to detect APTs in encrypted network traffic.


#TODO
** Code for creating live_capture pcap if it doesnt exist already
1) Get the dataset (features).
2) Select the features and code to extract those features from DS.
3) Update the model which should work for these features.
4) Optimise the command exec part if possible.
